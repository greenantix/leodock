# Extended dependencies for LeoDock Enhanced Platform

# Core web framework
Flask>=2.0

# HTTP requests
requests>=2.25

# pyxtermjs is assumed to be installed as per the original setup,
# but if it were a PyPI package, it would be listed here.
# pyxtermjs

# For database (sqlite3 is part of Python standard library)
# No explicit package needed for sqlite3 itself.

# Other potential useful libraries for future enhancements:
# python-dotenv # For loading environment variables from .env files
# websockets    # If real-time communication beyond basic polling is needed
# asyncio       # Already used in llm_manager, ensure Python 3.7+